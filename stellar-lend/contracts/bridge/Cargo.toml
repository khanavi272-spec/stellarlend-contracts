[package]
name = "bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
ed25519-dalek = { version = "1.0.1", features = ["std"] }
rand = "0.7"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
anyhow = "1.0"

[dev-dependencies]
hex = "0.4"
