# Legacy reference (`scr`)

This directory is a misnamed reference tree (`scr` instead of `src`). The canonical lending contract lives at `stellar-lend/contracts/lending`. Port interest and borrow changes into that crate only.
